/**
 * useGameEvents — subscribes to GameEngine events.
 * Toast notifications disabled (Sprint 16).
 */
export function useGameEvents() {
  // Reserved for future non-toast event handling (CAP scramble alerts, etc.)
}
